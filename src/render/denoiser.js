/** Device-local a-trous pass scheduler; raw accumulation is never overwritten. */
export class Denoiser {
 constructor(device){this.device=device;this.buffers=[];this.uniforms=[];}
 async init(){const code=await(await fetch(new URL('./denoise.wgsl',import.meta.url))).text(),module=this.device.createShaderModule({code,label:'Edge-aware a-trous denoiser'}),info=await module.getCompilationInfo();if(info.messages.some(m=>m.type==='error'))throw Error(info.messages.map(m=>m.message).join('\n'));this.pipeline=await this.device.createComputePipelineAsync({layout:'auto',compute:{module,entryPoint:'main'}});return this;}
 resize(width,height){if(this.width===width&&this.height===height)return;this.buffers.forEach(b=>b.destroy());this.uniforms.forEach(b=>b.destroy());this.width=width;this.height=height;this.buffers=[0,1].map(i=>this.device.createBuffer({label:`Denoise ping ${i}`,size:width*height*16,usage:GPUBufferUsage.STORAGE|GPUBufferUsage.COPY_SRC}));this.uniforms=[1,2,4,8,16].map(step=>{const b=this.device.createBuffer({size:16,usage:GPUBufferUsage.UNIFORM|GPUBufferUsage.COPY_DST});this.device.queue.writeBuffer(b,0,new Uint32Array([width,height,step,0]));return b;});}
 encode(encoder,source,aovs,iterations=3){iterations=Math.max(1,Math.min(5,iterations));for(let i=0;i<iterations;i++){const output=this.buffers[i%2],group=this.device.createBindGroup({layout:this.pipeline.getBindGroupLayout(0),entries:[{binding:0,resource:{buffer:this.uniforms[i]}},{binding:1,resource:{buffer:source}},{binding:2,resource:{buffer:aovs}},{binding:3,resource:{buffer:output}}]}),pass=encoder.beginComputePass({label:`Denoise a-trous ${1<<i}`});pass.setPipeline(this.pipeline);pass.setBindGroup(0,group);pass.dispatchWorkgroups(Math.ceil(this.width/8),Math.ceil(this.height/8));pass.end();source=output;}return source;}
 dispose(){this.buffers.forEach(b=>b.destroy());this.uniforms.forEach(b=>b.destroy());}
}
